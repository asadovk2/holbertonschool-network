#!/usr/bin/env bash
# This script displays all active IPv4 addresses on the machine, including localhost

set -euo pipefail

# List all IPv4 addresses, extracting the IP without the subnet
ip -4 -o addr show | awk '{split($4, a, "/"); print a[1]}'
