#!/usr/bin/env bash
# This script displays all active IPv4 addresses on the machine, including localhost

set -euo pipefail

# Print each IPv4 address on a separate line
for ip in $(hostname -I); do
    echo "$ip"
done
